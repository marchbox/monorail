---
layout: base.njk
override:tags: []
title: Drawings
eleventyNavigation:
  key: Drawings
  order: 3
---

<div class="drawings margin-trim" role="none">
  <h1 id="title">Drawings</h1>

  <section class="drawings-gallery">
    {% for drawing in collections.drawing|reverse %}
    {% set title_id = 'drawing-' + drawing.data.key %}
    {% set is_featured = loop.first %}
    {% set classes = {
      'drawing': true,
      'drawing-featured': is_featured
    }|classes %}
    <article class="{{classes}}" aria-labelledby="{{title_id}}">
      <a href="{{drawing.url}}">
        <figure>
          {% drawingImage drawing.data %}
          <figcaption class="margin-trim">
            {% if is_featured %}
            <p class="drawing-caption">{{drawing.data.caption}}</p>
            <p class="drawing-title" id="{{title_id}}">{{drawing.data.pageTitle}}</p>
            <p class="drawing-date">
              <time datetime="{{drawing.date|attrDate}}">
                {{drawing.date|readableDate}}
              </time>
            </p>
            {% else %}
              {{drawing.data.caption}}
            {% endif %}
          </figcaption>
        </figure>
      </a>
    </article>
    {% endfor %}
  </section>
</div>
